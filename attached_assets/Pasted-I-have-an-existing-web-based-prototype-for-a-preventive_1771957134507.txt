I have an existing web-based prototype for a preventive diabetes risk assessment tool. The UI currently consists of a "New Assessment" form and a "Patient History" table, as shown in the attached images (image.png files). Based on the project requirements and the provided dataset (diabetes_dataset.csv), please update the UI and its underlying logic to incorporate the following changes. The tool should now support both clinician and patient views, include all necessary input fields, and display results clearly.

Files to Edit:
index.html (or the main HTML file)

style.css (or any CSS file)

script.js (or any JavaScript file that handles form submission, risk calculation, and history display)

If a backend exists (e.g., Flask, Node.js), please update the relevant API endpoints to handle new inputs and return appropriate data.

Changes Required:
1. Add Missing Input Field: HbA1c
Add a numeric input field for HbA1c (%).

Placeholder: e.g. 5.7

Constraints: min="3" max="15" step="0.1"

2. Update Smoking History Dropdown
Replace the current dropdown (which only shows "Current Smoker") with a dropdown containing all categories from the dataset:

never

No Info

current

former

ever

not current

3. Fix BMI Default and Placeholder
The BMI field currently shows 0.3 and placeholder e.g. 5.5. Change to:

Default empty

Placeholder: e.g. 25.0

Add client-side validation: BMI must be between 10 and 60.

4. Redesign the History Table
The current table mixes patient profile values with risk scores. Restructure the table to clearly separate actual patient metrics (the values entered at that time) from the calculated risk score.

Suggested columns:

Date

Age

BMI

HbA1c

Blood Glucose

Hypertension (Yes/No)

Heart Disease (Yes/No)

Smoking History

Risk Score (single percentage, e.g., "23.5%")

Category (Low/Moderate/High)

If you prefer to keep a "Patient Profile" column, ensure it only contains the actual values, not a mix of "risk score" sub‑items.

5. Add Confidence / Uncertainty for Clinicians
In the history table (or in a tooltip next to the risk score), include a confidence measure.

Example: 23.5% (95% CI: 20–27%) or simply show the probability and add a note like "Model confidence: 0.85".

This can be derived from the logistic regression model's predicted probability (if available) or a simple heuristic.

6. Create Separate Views for Clinicians and Patients
Add a toggle button or two tabs: "Clinician View" and "Patient View".

Clinician View: Display detailed risk score, contributing factors (e.g., top 3 features with coefficients), and suggested follow-up actions (e.g., low: monitor annually; moderate: lifestyle counseling; high: refer for diagnostic testing).

Patient View: Show a simplified explanation:

Risk category (Low/Moderate/High) in plain language.

Top 2–3 modifiable risk factors in simple terms (e.g., "Your BMI is high", "Your blood glucose is elevated").

High‑level preventive advice (e.g., "Increase physical activity, reduce sugar intake, consult your doctor").

Ensure the patient view avoids medical jargon and does not present a numerical probability if that might be confusing.

7. Add a Visualization Section
Below the form or on a separate "Insights" tab, include simple charts:

Feature Importance Bar Chart: Show the top features (coefficients from the logistic model) that influence risk.

Risk Distribution Histogram: If history data is available, plot a histogram of predicted risks for all patients, optionally coloring by actual outcome.

Use a client‑side charting library (e.g., Chart.js) or generate images on the backend (e.g., using matplotlib) and serve them.

8. Add Input Validation
Implement front‑end validation for all numeric fields:

Age > 0 (integer)

BMI between 10 and 60

HbA1c between 3 and 15

Blood glucose between 50 and 400

Display helpful error messages if inputs are out of range.

9. Add a Disclaimer
At the bottom of the page, include a disclaimer:
"This tool is a prototype for decision support only. It does not provide a medical diagnosis. Always consult a healthcare professional."

10. Update Backend / Risk Calculation
text
- If the backend uses a pre‑trained logistic regression model, ensure it includes `HbA1c` as a feature.
- The model should return both the risk probability and the top contributing factors (e.g., feature coefficients multiplied by the patient's feature values).
- The backend should also be able to return data for visualizations (e.g., feature importance coefficients, risk distribution).